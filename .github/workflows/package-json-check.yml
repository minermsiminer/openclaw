name: 'Package JSON validation'

on:
  push:
    paths:
      - 'package.json'
      - 'scripts/validate-package-json.cjs'
  pull_request:
    paths:
      - 'package.json'
      - 'scripts/validate-package-json.cjs'

jobs:
  validate-package-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run package.json validation
        run: node scripts/validate-package-json.cjs
